<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Portourgal.View.RoteirosView">
    <ContentPage.Content>
        <StackLayout Margin="15">
            <BoxView BackgroundColor="Aqua" WidthRequest="10" HeightRequest="300" />
            <Label Text="Percurso:" TextColor="Black" FontSize="20" HorizontalOptions="Start" VerticalOptions="Center"/>
            <StackLayout BindableLayout.ItemsSource="{Binding Cidades}" HorizontalOptions="Start">
                <Label Text="{Binding .}" TextColor="Black" FontSize="16" HorizontalOptions="Start"/>
            </StackLayout>
            <BoxView BackgroundColor="Transparent" HeightRequest="20"/>
            <Label Text="Descrição" TextColor="Black" FontSize="20" HorizontalOptions="Start" VerticalOptions="Center"/>
            <Label Text="{Binding Descricao}" TextColor="Black" FontSize="16" HorizontalOptions="Start" VerticalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>